# Scene / VTools

| 项 | 值 |
|----|----|
| 显示名 | Scene（VTools） |
| 包名 | `com.omarea.vtools` |
| 模块版本 | ZoeVIP 3.8.0（versionCode 99） |
| 入口 | `VToolsHooks`（legacy） / `LibVToolsHooks`（libxposed） |

## 说明

从微信恢复的 `ZoeVIP-3.8.0.apk` 回填工程；源码自历史会话与安装包对照恢复。

LSPosed 作用域需勾选 `com.omarea.vtools`，强制停止 Scene 后重开。
